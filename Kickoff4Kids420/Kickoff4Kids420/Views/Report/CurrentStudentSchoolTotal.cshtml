@model IEnumerable<Kickoff4Kids420.ViewModels.ReportViewModel>


@{
    ViewBag.Title = "CurrentStudentSchoolTotal";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="breadcrumbs margin-bottom-40">
    <div class="container">
        <h1 class="pull-left">Current Student Unspent Points Per School</h1>
    </div>
</div>
<div class="container">
<div class="panel panel-red">
    <div class="panel-heading">
        <h1 class="panel-title">School Unspent Points</h1>
    </div>
    <table class="table table-striped table-hover table-bordered">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SchoolName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.State)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalPointsPerSchool)
            </th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.SchoolName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.State)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TotalPointsPerSchool)
                </td>
            </tr>
        }

    </table>
</div>
</div>
