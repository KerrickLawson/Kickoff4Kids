
@model IEnumerable<Kickoff4Kids420.Models.Product>

@{
    ViewBag.Title = "Index";
}
@section Styles {
    @Styles.Render("~/pricing/css")
}
@*<h2>Available Products</h2>
<img src="~/Images/school%20reward.jpg" />
<table class="table table-striped table-hover table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProductName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Categories.CategoryName)
        </th>
        <th>
            Purchase Item
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProductName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Categories.CategoryName)
            </td>
            <td>
                @Html.ActionLink("Add to Cart", "AddToCart", "ShoppingCart", new { id = item.ProductId }, "")
            </td>
        </tr>
    }

</table>*@

<div class="breadcrumbs margin-bottom-40">
    <div class="container">
        <h1 class="pull-left">Available Products</h1>
    </div>
</div>
<div class="container">
    <!-- Pricing -->
    @*<div class="row margin-bottom-40">*@
@foreach (var item in Model)
{
    <div class="col-md-3 col-sm-6">
        <div class="pricing hover-effect">
            <div class="pricing-head">
                <h3>@item.ProductName @*<span>@item.</span>*@</h3>
                <h4>@item.Price <span>Points</span></h4>
            </div>            
            @if (item.PictureUrl != null)
        {
            <ul class="pricing-content list-unstyled hidden-phone hidden-sm hidden-xs product-img">
                <li> <img alt="@item.ProductName" src="@item.PictureUrl" /> </li>
            </ul>
        }
    @if (item.PictureUrl == null)
        {
             <ul class="pricing-content list-unstyled hidden-phone hidden-sm hidden-xs product-img">
                <li> <img alt="@item.ProductName" src="/Content/Images/question-mark.jpg" /> </li>
               </ul>
        }
            <div class="pricing-footer">
                @*<p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Ut non libero magna psum olor .</p>*@
                @*<button type="submit" class="btn-u-red">@Html.ActionLink("Add to Cart", "AddToCart", "ShoppingCart", new { id = item.ProductId }, "")</button>*@
                <a length="0" href="/ShoppingCart/AddToCart/@item.ProductId" class="btn-u-red">Add to Cart</a>

            </div>
        </div>
    </div>
    
}
   
     </div>
@*</div>*@
